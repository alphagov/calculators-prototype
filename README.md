A very (very, very) early prototype / experiment into what a calculator might look like.

```
$ bundle
$ gem install shotgun
$ shotgun -p 3000
```

Visit `localhost:3000/child-benefits` to see an example.
